#include<stdio.h>
int main(){
    int l,b,area;
    printf("Enter the length and breadth of the reactangle:\n");
    scanf("%d%d",&l,&b);
    area=l*b;
    printf("The area of the rectangle is %d\n",area);
    return 0;
}

#include<stdio.h>
int main(){
    int b,h,area;
    printf("Enter the base and height of the triangle: ");
    scaanf("%d%d,&b,&h");
    area = 0.5*b*h;
    printf("The area of the triangle is %d",area);
    return 0;
}
